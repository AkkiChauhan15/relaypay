{
  "name": "relaypay",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "build": "npm run build --workspace backend && npm run build --workspace mobile",
    "format": "npm run format --workspace backend && npm run format --workspace mobile",
    "format:check": "npm run format:check --workspace backend && npm run format:check --workspace mobile",
    "lint": "npm run lint --workspace backend && npm run lint --workspace mobile",
    "test": "npm run test --workspace backend && npm run test --workspace mobile"
  },
  "engines": {
    "node": ">=22.11.0"
  }
}
